26-04-2026 Today I learned that having a proper directory structure is a killer feature before your actually write a single line of code
27-04-2026 Today I dedicated the model needs a defined service level contract which needs to be refreshed to keep the gyroscope of collaboration spinning. The file is called FZ_01_AI_ServCon.txt
27-04-2026 not outsourcing string and path dependencies to config files is a huge load later source of bugs and effort
28-04-2026 Consolidated all Gestalt path construction into a single utility (getGestaltFileUrl) — 6 files cleaned up, one duplicate function deleted. MAP principle applied: one source of truth beats elegant repetition.
28-04-2026 The ServCon is just a profile whose subject happens to be an AI. The same data primitives (profile/token/sticky) cover behavioral preferences for people, places, and AI interaction style equally. No special editor needed.
28-04-2026 A PUCK compiler needs render targets — same data, different outputs (AI prompt vs human brief vs decision input). Data model stays generic; compilation is where the target-specific logic lives.
28-04-2026 The right mental model for HUM/AI collaboration is Tennis Doubles — same side, shared score, situational roles, peer level. ServCon is the pre-match agreement. PUCK is the court briefing. HUM provides positioning, AI provides range. Neither wins alone.
28-04-2026 To restart just the backend without killing context: Get-NetTCPConnection -LocalPort 3001 | OwningProcess | Stop-Process, then Start-Process cmd with node backend.cjs
28-04-2026 PuckCreator now includes an optional Flow Calibration block in the PUCK export — plain text, one param per line. Any AI can read it cold. No API, no memory, no platform dependency.
28-04-2026 The PUCK workflow row makes the sequence explicit in the UI: Show Config (calibrate the BioLogic) → Show Gestalt (verify what's in scope) → Create PUCK (compile and export). Order matters — it's a prep chain, not a menu.
28-04-2026 BioLogic coined: the explicit, portable operating system beneath prompts. What FlowZero makes machine-readable. AI-agnostic — travels with the person, not the platform.
29-04-2026 PUCK Library built: fz_puck_index.json stores compiled PUCK registry. PuckLibrary.jsx reads it — shows name, visibility, date, editable description, per-row download. Save Index persists description edits.
29-04-2026 JobPackageComposer built: free-text job brief + checkbox PUCK selection → generates CompositePUCK .txt. Tested with Pia (fz0001): AI cold-read the food constraints, social graph, and friend dynamics with zero explanation from the user. Loop working as designed.
29-04-2026 The refinement loop is the product. Result → refine at whichever layer is cheapest (brief, sticky, token, PUCK selection). Each iteration improves the artifact, not the prompt. Cold AI gets a better brief next time without re-explaining anything. MAP principle in operation.
29-04-2026 "Stay flowy and fuzzy and still wobble forward" — the human operating mode FlowZero is designed for. Not a system that demands precision upfront. A system that captures precision incrementally.