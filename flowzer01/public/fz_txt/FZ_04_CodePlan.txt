
# [2026-04-29] Next Steps — V1 Enhancement Plan

## Step 1: Sticky Type Convention — "gap" and "fault" ✓ COMPLETE (2026-04-29)
- No code change. Convention only.
- Reserved stickytype values defined: "gap" (missing/unverified info), "fault" (active incident).
- Documented in FZ_02_Appdocu.txt (Main Components section) and FZ_88_App_Glossary.txt.
- StickyEdit UI already supports free-text stickytype — no UI change needed.

## Step 2: PUCK Save to fz_PUCK_data/
- Add backend endpoint: POST /api/save-puck — writes .txt file to public/fz_PUCK_data/.
- Add backend endpoint: GET /api/list-puck-files — lists .txt files in fz_PUCK_data/.
- PuckCreator: add [Save to Library] button alongside existing [Export] (download) button.
- No UI redesign. Single new button, single new file write.

## Step 3: PUCK Library Index
- Create fz_puck_index.json in public/fz_PUCK_data/: array of { name, sourceProfiles[], visibility, date, filename }.
- Add backend endpoint: POST /api/save-puck-index.
- PuckCreator writes index entry on [Save to Library].
- No new UI screen yet — index is infrastructure for Step 4.

## Step 4: JobPUCK Composer Screen
- New component: JobPuckComposer.jsx.
- UI: multi-select from PUCK Library (populated from fz_puck_index.json) + free-text task sticky + visibility selector + [Compile JobPUCK] button.
- Compiler: merges selected profile/token/sticky arrays, deduplicates, appends task sticky, exports as single .txt.
- Add to toolbar Row 2 or as a new Row 3.

## Step 5: CCML Token Field in Token Editor
- Add optional "ccml" field to token data model: { Token, Meaning, Usage, ccml, flag }.
- TokenEdit.jsx: add input for CCML expression alongside existing fields.
- No backend change — field rides in existing fzXXXX_token.json.
- PUCK export includes ccml field if present. AI can use it for precise entity resolution.

---

# [2026-04-28] PuckCreator — Flow Calibration Integration Complete

- Added `includeFlowConfig` checkbox and `flowConfig` state to PuckCreator.jsx.
- On mount, loads FZ_Flow_Config.json via getGestaltFileUrl. Checkbox disabled with note if file missing.
- handleExport now: uses getGestaltFileUrl for loader readme; builds a FLOW CALIBRATION plain-text block
  (param label: value, one per line) if checkbox checked; concatenates loader + flow block + JSON.
- Export format: human + AI readable. No API dependency. Works in any AI session by paste.

---

# [2026-04-28] UI Toolbar Restructure — PUCK Workflow Row

- Moved "Calibrate Flow" and "Show Gestalt" out of the main editor button row.
- New Row 2: [Show Config] → [Show Gestalt] → [Create PUCK] — left-to-right workflow progression.
- Show Config and Show Gestalt: solid #15396a blue (white text). Create PUCK: light blue fill (dark text).
- Removed PuckCreatorWrapper import from App.jsx; PUCK panel now rendered inline with showPuck state.
- PuckCreatorWrapper.jsx still exists but is no longer used by App.jsx (can be deleted in future cleanup).

---

# [2026-04-28] MAP — Design Goal Tightened

Munchhausen Architecture Principle design goal (locked):
  "Compress definitions until reconstruction becomes trivial."

This is an optimization target, not just a description. It means:
- If a cold reader (human or AI) must infer anything to reconstruct the system, the artifact is underspecified.
- fz_txt/ is the MAP artifact store. Every non-obvious decision must have a trace here or in config.json.
- Reconstruction requires zero prior context — only assembly.

Apply this criterion when reviewing or updating any fz_txt/ file:
  Q: Can someone with no history rebuild this from only the artifact set?
  If no → compress and specify further.

---

# [2026-04-28] BioLogic — Term Locked

- BioLogic = the explicit, portable operating system beneath prompts.
- Made machine-readable via Flow Calibration (FZ_Flow_Config.json) + PUCK export.
- AI-agnostic by design: any model can ingest it cold. Not tied to API memory, personas, or fine-tuning.
- Contrast: most "AI personalization" is platform-side. BioLogic travels with the person, not the platform.

---

# [2026-04-28] ServCon — Keep as .txt for Now

ServCon (FZ_01_AI_ServCon.txt) stays as a free-text file intentionally.
Reason: still in learning mode — the right fields, values, and structures are not yet
known. Free text allows discovery without premature schema lock-in.

Future migration trigger: when interaction patterns stabilize and recurring config
combinations emerge (e.g. always using the same mode + rules for coding sessions),
convert to structured JSON config files with selectable values.

Target end state:
  - Named collab profiles as separate JSON files: Playmode.json, Analysis.json, Debug.json, etc.
  - Each file holds a set of ServCon parameters (mode, rules active, signals, escalation level)
  - User picks a profile at session start; system compiles it into the upload-ready .txt format
  - Editor: same Jedit-style panel, no special UI needed

Do not migrate until at least 3-4 stable "modes" have been used repeatedly in practice.
Premature structuring of fluid preference data is a known failure mode.

---

# [2026-04-28] PUCK Compiler — Future Requirement Note

## Compilation Target Separation

The PUCK compiler currently exports a raw data dump.
Future requirement: the compiler needs to support named *render targets* — the same
profile data compiled differently depending on intended use.

Examples:
  - target: "ai_prompt"      → renders as behavioral instructions for LLM priming
  - target: "human_brief"    → renders as plain-language summary for a person
  - target: "decision_input" → renders as constraint list for a task (e.g. restaurant selection)

This is a compiler concern, NOT a data model concern.
The underlying profile/token/sticky primitives do not change.
A profile describing AI interaction style and a profile describing a person's dietary
preferences use the same data shape — only the render target differs.

No code change needed now. Revisit when PUCK export is beyond partial status.

---

# [2026-04-28] Path Utility Refactor Complete

- Added getGestaltFileUrl(filename) to userIndexUtil.js — single source of truth for
  building any Gestalt-directory fetch URL.
- Removed inline 3-line path construction from: App.jsx, Jedit.jsx, TokenEdit.jsx,
  StickyEdit.jsx, GestaltViewer.jsx, PuckCreator.jsx.
- Removed duplicate fetchProfileIndex() from IndexEdit.jsx; now uses fetchUserIndex().
- All imports updated from getFZ_GPATH → getGestaltFileUrl where applicable.

---

# [2026-04-27] Path Refactor Complete

- All Gestalt directory references are now centralized in config.json (FZ_GPATH).
- Refactor tested: renaming the directory and updating config works after server restart.
- Next: Apply this pattern to other path dependencies as the codebase grows.

At start: batchfile called meup.bat (terminal "start meup") this starts the servers

- refactor .\Gestalt references to FZ_GPATH variable in config.json
